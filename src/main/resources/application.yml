spring:
  redis:
    host: 127.0.0.1
    port: 6379
    cache:
      type: redis
      redis:
        time-to-live: 30s
        
  datasource:
    url: jdbc:mysql://localhost/customers
    username: root
    password: rootpassword
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
